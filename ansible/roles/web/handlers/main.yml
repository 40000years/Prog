# ============================================================
# Web Role - Handlers
# ============================================================

- name: Restart Nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted

- name: Restart Node App
  ansible.builtin.systemd:
    name: myapp
    state: restarted
    daemon_reload: true
